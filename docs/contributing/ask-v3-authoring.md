# Ask V3 Authoring

`deck ask` no longer uses the v3 action loop as the default authoring path.

Use `docs/contributing/ask-agent-runtime.md` for the current runtime.

This file remains as historical context for the earlier bounded action-schema loop that bridged the planner/compiler pipeline toward the current agent runtime.
