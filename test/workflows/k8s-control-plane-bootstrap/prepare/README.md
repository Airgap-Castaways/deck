# Prepare assets

This directory is the canonical place for bootstrap-specific prepare assets.

It is intentionally empty today because the current bootstrap scenario resolves its inputs from `profile/`, `apply/`, `vars/`, and `_shared/k8s/`.
