# Prepare assets

This directory is the canonical place for worker-join prepare assets.

It is intentionally empty today because the current join scenario resolves its inputs from `profile/`, `apply/`, `vars/`, and `_shared/k8s/`.
