# Prepare assets

This directory is the canonical place for node-reset prepare assets.

It is intentionally empty today because the current reset scenario resolves its inputs from `profile/`, `apply/`, `vars/`, and `_shared/k8s/`.
