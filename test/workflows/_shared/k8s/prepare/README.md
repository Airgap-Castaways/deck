# Shared prepare fragments

This directory marks the canonical location for reusable Kubernetes prepare assets under `test/workflows/_shared/k8s/`.

It is intentionally empty today because the current scenarios only share `apply/` and `vars/` fragments.
